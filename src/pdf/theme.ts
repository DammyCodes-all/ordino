import { THEME } from "./professional-theme";

export {
  THEME,
  spacingMap,
  emphasisStyle,
  resolveHeadingStyle,
  resolveParagraphStyle,
  resolveListNodeStyle,
  resolveTableStyle,
  resolveQuoteStyle,
  resolveCalloutStyle,
  resolveDividerStyle,
} from "./professional-theme";

export const PAGE_SIZE = THEME.PAGE_SIZE;
export const PAGE_WIDTH_PT = 595.28;
export const PAGE_HEIGHT_PT = 841.89;
export const DEFAULT_MARGIN = THEME.MARGIN.left;
export const DEFAULT_FONT_FAMILY = "Helvetica";
export const DEFAULT_FONT_SIZE = THEME.FONT_SIZES.body;
